server.port: 9000

camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Demo
  filter:
    create: All tasks
  auto-deployment-enabled: false

spring:
  datasource:
    url: "jdbc:mysql://localhost/camunda-simulator?useUnicode=yes&characterEncoding=UTF-8&serverTimezone=UTC&autoReconnect=true"
    username: "root"
    password: "root"
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
